# Claude Instructions

Use [AGENTS.md](AGENTS.md) as the canonical instruction file for this repository.

Most user edits should touch only `content/` and `data/`. Run `pnpm pagepilot:check` before final response.
